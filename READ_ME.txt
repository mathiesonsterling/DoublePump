new features:
    - add time delay between runs
    - add DI import of external classes for actual use

later:
  - add paralizable task paths when possible
  - dynamic generation of tasks from code rather than instance
  - repeatable tasks?
  - per task type resource
